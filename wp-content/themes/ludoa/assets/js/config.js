/* ============================================================
   Cấu hình toàn site.
   ============================================================ */
window.SITE_CONFIG = {
  // URL hệ thống đặt phòng — dùng chung cho mọi nút đặt phòng.
  // CẬP NHẬT SAU (khách hàng sẽ cấp link). Tạm thời để '#'.
  BOOKING_URL: "#",
};
